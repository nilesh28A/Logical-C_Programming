#include<stdio.h>
int main(){
    int side;
    printf("enter side os square:"); // circal, ractangle, triangle, etc...
    scanf("%d",&side);
    
    printf("area is:%dmm",side*side);
    return 0;
}